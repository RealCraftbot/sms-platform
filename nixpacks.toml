[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
nixPkgs = ["openssl"]
cmds = ["npm ci"]

[phases.build]
nixPkgs = ["openssl", "pkg-config"]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "npm run start"

[variables]
NODE_ENV = "production"